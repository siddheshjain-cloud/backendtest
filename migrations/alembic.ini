[alembic]
script_location = %(here)s
prepend_sys_path = ..
path_separator = os

# Intentionally no sqlalchemy.url. env.py resolves the target from the running
# Flask application's SQLALCHEMY_DATABASE_URI, or from an explicit programmatic
# sqlalchemy.url set by the migration test helpers. Never put a deployment URL
# or credentials in this tracked file.
